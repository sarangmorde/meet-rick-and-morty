<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.meetrickandmorty.app">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_rick_and_morty_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_rick_and_morty_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.MeetRickAndMorty">
        <activity android:name="com.meetrickandmorty.app.ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>